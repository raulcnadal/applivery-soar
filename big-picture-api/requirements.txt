# Local/editor copy — kept in sync with the root requirements.txt, which is
# the one the Dockerfile actually installs from. See the comment at the top
# of that file for why: main.py has one hard top-level import
# (cryptography) plus several soft/optional ones (asyncpg, boto3, paramiko)
# and two lazily-imported PDF-generation ones (jinja2, weasyprint,
# matplotlib). playwright was removed — nothing in main.py imports it.
fastapi==0.135.3
uvicorn[standard]==0.42.0
httpx==0.28.1
pydantic==2.12.5
pyjwt==2.12.1
cryptography==43.0.3
jinja2==3.1.6
weasyprint==68.1
matplotlib==3.10.8
asyncpg==0.31.0
boto3
paramiko
