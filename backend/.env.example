NODE_ENV=development
PORT=8000

# Required — process refuses to start without it (mirrors DASHBOARD_SECRET).
DASHBOARD_SECRET=change-me-to-a-long-random-string

# Required — Postgres connection string.
DATABASE_URL=postgresql://soar:soar@localhost:5432/soar?schema=public

APPLIVERY_API_URL=https://api.applivery.com

# Optional — separate secret gating /api/compliance/evaluate-due for
# deployments that prefer an outside cron to drive evaluation.
TRIGGER_SECRET=

CORS_ORIGINS=*
