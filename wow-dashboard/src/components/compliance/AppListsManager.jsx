// Deprecated: replaced by ./AppListsView.jsx (full dedicated page view,
// reachable only via the "App Lists" button in CompliancePoliciesView).
// This file is intentionally left as an inert stub — the App Lists modal
// UI it used to export is no longer imported anywhere. Safe to delete.
export default null;
